export interface TrailerInfo {
  title: string;
  subtitle: string;
  axles: number;
  capacity: string;
  length: string;
  extension: string;
  workingArea: string;
  loadingHeight: string;
  imageUrl: string;
}
